{% extends 'base.html.twig' %}

{% block title %}Categories List{% endblock %}

{% block body %}
    <div class="row">
    <h1 class="text-center mt-5 p-3">Here is displayed all Categories</h1>
        {% for category in categories %}
            <div class="col-sm-12 col-md-12 col-lg-4 col-xl-4">
                <div class="card mb-3 border-3 border-primary  border-collapse">
                    <h4 class="card-header border-bottom border-primary"> {{category.label}} </h4>
                    <div class="card-body bg-dark text-center border-bottom border-3 border-primary">
                        <img class="" src="{{category.image}}" alt="Avatar">
                    </div>
                    <a href=" {{path('category' , {'id': category.Id } )}} " class="btn btn-info text-center m-2 ">Détails</a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
